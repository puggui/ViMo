<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="#">
  <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
  <script src="/js/bootstrap.js"></script>
  <title>Sign In</title>
</head>

<body>
  <%- include('navbar'); -%>
    <div class="container">
      <%- include('flash'); -%>
        <div class="d-flex justify-content-center">
          <div class="" style="width: 18rem; margin-top: 6rem;">
            <img class="card-img-top" src="<%= movie.MOVIE_POSTER %>" alt="Card image cap">
          </div>
          <div class="" style="width: 26rem; margin: 6rem 0 0 3rem;">
            <div class="card-body">
              <h2 class="card-title mb-3">
                <%= movie.MOVIE_TITLE %> (<%= movie.MOVIE_YEAR %>)
              </h2>
              <h6 class="card-subtitle">Director</h6>
              <p class="card-text">
                <%= movie.MOVIE_DIRECTOR %>
              </p>
              <h6 class="card-subtitle">Genre</h6>
              <p class="card-text">
                <%= movie.MOVIE_GENRE %>
              </p>
              <h6 class="card-subtitle">Plot</h6>
              <p class="card-text">
                <%= movie.MOVIE_PLOT %>
              </p>
              <button type="button" class="btn btn-primary">Add to Cart</button>
              <a href="/movie/<%= movie.MOVIE_ID %>/edit" class="btn btn-secondary">Edit</a>
              <form action="/movie/<%= movie.MOVIE_ID %>" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger">Delete</button>
              </form>
            </div>
          </div>
        </div>
    </div>

</html>