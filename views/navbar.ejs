<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <% if (currentUser && currentUser.USER_ISADMIN) { %>
    <a class="navbar-brand ps-3" href="/">Admin Panel</a>
    <% } else { %>
    <a class="navbar-brand ps-3" href="/">ViMo</a>
    <% } %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/aboutus">About Us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/contact">Contact Us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/cart">Cart</a>
        </li>
        <% if (currentUser && currentUser.USER_ISADMIN) { %>
        <li class="nav-item">
          <a class="nav-link" href="/movie/add">Add Movie</a>
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav ml-auto mb-2 mb-lg-0">
        <form class="d-flex pe-3" role="search" action="/search" method="GET">
          <select class="form-select me-2" name="option" style="width: 12rem;">
            <option value="">Search by ...</option>
            <option value="title">Search by title</option>
            <option value="director">Search by director</option>
            <option value="genre">Search by genre</option>
            <option value="plot">Search by plot</option>
          </select>
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="query" style="width: 13rem;" >
          <button class="btn btn-outline-secondary" type="submit">Search</button>
        </form>
    
        <% if (!currentUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/register">Register</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/signin">Sign In</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Log out</a>
        </li>
        <span class="navbar-text px-3">
          Welcome back, <%= currentUser.USER_NAME %>
        </span>      
        <% } %>
      </ul>
    </div>
  </div>
</nav>